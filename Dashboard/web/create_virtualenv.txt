These are example commands to setup the virtualenv:

rm -rf ../covid_app_venv
python3 -m venv ../covid_app_virtualenv
source ../covid_app_virtualenv/bin/activate
pip install --upgrade pip
pip install flask
pip install sqlalchemy
pip install flask-sqlalchemy
pip install requests
pip install dash
pip install Flask-WTF
pip install pandas